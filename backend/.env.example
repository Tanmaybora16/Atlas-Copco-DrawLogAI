# Database Configuration
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=root
DB_NAME=error_db

# SMTP Email Configuration
# Production Defaults (port 25 usually does not need a password):
EMAIL_SENDER=Errorloggingportal@atlascopco.com
EMAIL_PASSWORD=
SMTP_SERVER=smtp.onevirtualoffice.local
SMTP_PORT=25
SMTP_USE_TLS=false
